# For Honor Helper

This Python script captures the center half of your Windows screen at high speed using `mss`.

For better perfomance reduce screen resolution.

## Setup

1. Install dependencies:

```
pip install -r requirements.txt
```

## Usage

Run the script:

```
python main.py
```
